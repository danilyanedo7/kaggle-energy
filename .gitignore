# Quarto build artifacts (regenerated by `quarto render`)
/_site/
/.quarto/
/_freeze/
*_files/
*.knit.md
*.utf8.md

# Raw Kaggle data: not redistributed, regenerate via R/01_clean_data.R
# after downloading the two CSVs (see README) -- data/processed/ (small,
# derived, no license concerns) IS committed so the site can render
# without needing the raw files or Kaggle credentials.
/data/raw/

# R
.Rhistory
.RData
.Rproj.user/
Rplots.pdf

# OS / editor cruft
.DS_Store

# Scratch/testing output, not part of the deliverable
/scratch/

**/*.quarto_ipynb
